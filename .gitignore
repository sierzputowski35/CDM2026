# Backups locaux créés par Claude Code avant les édits risqués
# (utile pour rollback rapide, pas pertinent pour le repo)
*.bak

# Config locale Claude Code (launch.json, etc.)
.claude/

# Doc de passation entre conversations (local, pas dans le repo)
AVANCEMENT.md
